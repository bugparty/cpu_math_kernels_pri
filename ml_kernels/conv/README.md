# Convolution Kernels

Use this directory for convolution-oriented kernels such as:

- direct convolution microkernels
- im2col or implicit-GEMM variants
- fused bias / activation epilogues
- data layout transforms

Recommended naming: `conv_<op>_<variant>.cpp`.
