# Attention Kernels

Use this directory for attention-related kernels such as:

- QK matmul microkernels
- softmax and masked softmax
- RoPE / positional encoding transforms
- KV-cache layout and decode kernels

Recommended naming: `attention_<op>_<variant>.cpp`.
